/*import Peer from '../../node_modules/simple-peer-light/simplepeer.min.js';
import {addLogMessage, getMyPeerId, signalMyPeer, step3} from './peer.js'

var peer = new Peer({
    //initiator: true, //qui inicia la trucada
    initiator: true,
    trickle: false,
    //stream: myStream,
});

var isInitiator = peer.initiator

// peer.on("signal", (data) => {
//     window.id2 = data
//     console.log("aaaafdbgfdngfn");
// });

// var id = window.id2

var peerClient = new PeerClient(peer, isInitiator)
peerClient.connection()

window.pc = peerClient
window.getId = getMyPeerId
window.signalPeer = signalMyPeer
window.step3 = step3

// window.log = addLogMessage
// addLogMessage("uwu")

*/